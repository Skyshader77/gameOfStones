export const MAX_NAME_LENGTH = 30;
export const MAX_DESCRIPTION_LENGTH = 100;

export const VALID_NAME_REGEX = /^[A-Za-z]+(?:[-' ][A-Za-z]+)*$/;

export const POWER = 2;
export const DIVISION_FACTOR = 2;
